#pragma once

#include <string>
#include <vector>

int test_upper_bound_index_cpp(const std::vector<int>& values, int target);
std::string test_upper_bound_index_cpp_output(
  const std::vector<int>& values,
  int target);
