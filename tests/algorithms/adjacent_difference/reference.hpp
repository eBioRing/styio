#pragma once

#include <string>
#include <vector>

std::vector<int> test_adjacent_difference_cpp(std::vector<int> values);
std::string test_adjacent_difference_cpp_output(const std::vector<int>& values);
