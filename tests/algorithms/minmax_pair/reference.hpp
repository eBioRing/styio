#pragma once

#include <string>
#include <utility>
#include <vector>

std::pair<int, int> test_minmax_pair_cpp(const std::vector<int>& values);
std::string test_minmax_pair_cpp_output(const std::vector<int>& values);
