values <- @stdin: list[i32]

answer = -1
n = values.length

?(n > 0) => {
    answer = values[0]
    ?(n > 1) => {
        [1..n-1] >> #(i) => {
            ?(values[i] < answer) => {
                answer = values[i]
            }
        }
    }
}

>_(answer)
