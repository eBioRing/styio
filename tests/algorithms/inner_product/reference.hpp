#pragma once

#include <string>
#include <vector>

int test_inner_product_cpp(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
std::string test_inner_product_cpp_output(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
