values <- @stdin: list[i32]

running = 0
n = values.length

?(n > 0) => {
    [0..n-1] >> #(i) => {
        current = values[i]
        values[i] = running
        running += current
    }
}

>_(values)
