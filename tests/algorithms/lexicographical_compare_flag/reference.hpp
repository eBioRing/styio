#pragma once

#include <string>
#include <vector>

int test_lexicographical_compare_flag_cpp(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
std::string test_lexicographical_compare_flag_cpp_output(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
