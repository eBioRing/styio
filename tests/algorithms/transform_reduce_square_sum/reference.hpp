#pragma once

#include <string>
#include <vector>

int test_transform_reduce_square_sum_cpp(const std::vector<int>& values);
std::string test_transform_reduce_square_sum_cpp_output(
  const std::vector<int>& values);
