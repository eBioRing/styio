#pragma once

#include <string>
#include <vector>

int test_subrange_search_index_cpp(
  const std::vector<int>& haystack,
  const std::vector<int>& needle);
std::string test_subrange_search_index_cpp_output(
  const std::vector<int>& haystack,
  const std::vector<int>& needle);
