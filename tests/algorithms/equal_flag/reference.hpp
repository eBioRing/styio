#pragma once

#include <string>
#include <vector>

int test_equal_flag_cpp(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
std::string test_equal_flag_cpp_output(
  const std::vector<int>& lhs,
  const std::vector<int>& rhs);
