l <- @stdin: list[i32]

n = l.length - 1

?(n > 0) => {
    [0..n] >> #(i) => {
        [0..n-i-1] >> #(j) => {
            ?(l[j] > l[j+1]) => {
                l[j], l[j+1] = l[j+1], l[j]
            }
        }
    }
}

>_(l)
